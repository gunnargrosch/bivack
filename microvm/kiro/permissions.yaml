# Image-managed permissions for this dedicated, per-user MicroVM.
# The MicroVM is the isolation boundary; Kiro CLI may execute supported tools
# without confirmation inside it.
rules:
  - capability: all
    effect: allow
