node_modules/
*.DS_Store
*.bak
/tmp/

# IDE (VS Code workbench) build output
ide/dist/
ide/.build-hash
frontend/.deploy-hash

# SAM build artifacts
.aws-sam/

# Local SAM CLI config — never commit (your AWS profile, personalized
# parameter_overrides, etc.). See samconfig.toml.example for the template.
samconfig.toml

# Local deploy config (AWS profile, region, login email). See deploy.env.example.
deploy.env
